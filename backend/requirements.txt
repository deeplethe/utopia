# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
mcp>=1.29,<2

# --- Config / settings ---
pydantic-settings>=2.4
python-dotenv>=1.0

# --- Relational metadata store ---
sqlmodel>=0.0.22
psycopg[binary]>=3.2

# --- Auth (password hashing) ---
bcrypt>=4.0
cryptography>=43.0

# --- LLM (OpenRouter) ---
httpx>=0.27

# --- Ontology: RDF store + manipulation + validation ---
rdflib>=7.0
pyoxigraph>=0.4
pyshacl>=0.26

# --- Document parsing fallbacks (docling is installed separately, it is heavy) ---
pypdf>=5.0
python-docx>=1.1
openpyxl>=3.1

# --- Semantic duplicate detection uses OpenRouter's /embeddings endpoint (no local
#     model needed); numpy comes transitively via docling's stack. ---
