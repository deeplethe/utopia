-- 这一轮认下了哪些实体：id、名字、类型。下一轮回放它。
--
-- 会话历史此前只回放 role + content，工具调用与结果一条都不进。于是第二轮
-- 开始时模型只看得见上一轮的最终答案文字，不知道自己搜过什么、拿到过哪些 id,
-- 只能从名字重搜一遍。实测就是这个样子：问"华生和夏洛克的关系"，再问一次
-- 换个名字，两轮各检索一遍。
--
-- **不回放整段工具结果**：那里面是 chunk 正文，每轮重复堆进上下文，几轮就把
-- 窗口吃光。要回放的是身份——有了 id，下一轮直接调 entity_facts，不必再从
-- 名字查起。顺带治掉一个更隐蔽的毛病：同名歧义时两轮可能查到**不同的实体**，
-- 于是前后两个答案讲的不是同一个节点。
--
-- 与抽取里 known_block 同一个套路（前面分块已认下的实体注入后续分块），
-- 理由也一样：让模型接着上一次的结论走，而不是每次从零开始猜。
ALTER TABLE conversation_messages
    ADD COLUMN resolved jsonb NOT NULL DEFAULT '[]'::jsonb;
