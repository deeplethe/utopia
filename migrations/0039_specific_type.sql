-- 模型对每个实体自己的说法：它认为这最具体是个什么。
--
-- 跟 proposed_type 是两回事，不能合用一列：那一列的含义是"模型要的东西本体里
-- 没有"，0003 的增长回路靠它的**稀有性**设门槛。每个实体都填的话，回路会给
-- 每一个实体提议建一个新类。
--
-- 为什么需要它：清单里总有个"差不多"的。本体有 product，模型觉得够用就选了，
-- 心里那个"向量数据库软件"就此丢失——实测 17 个实体的 proposed_type 全空。
-- 而类型消解最需要的正是这个名字：短名字对短标签，比拿一段中文散文去匹配
-- schema.org 的 "A software application." 近得多。
ALTER TABLE entities ADD COLUMN specific_type text;
