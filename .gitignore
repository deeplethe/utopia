/target
**/*.rs.bk
.env
node_modules/
web/dist/
*.log
.DS_Store
/data
# docs/ 是本地草稿区；只有决策记录入库。
# 必须写成 /docs/* 而非 /docs——父目录被忽略时，子路径无法用 ! 重新纳入。
/docs/*
!/docs/decisions/
.claude/

# 测量台与临时脚本的运行产物。**按前缀整段忽略**：
# 先前只列了 _bench_*，结果 _holmes2.json 混进了一次提交
_*.json
_*.err
_*.sse
_*.tmp
_*.out
_*.sh
_*.txt
